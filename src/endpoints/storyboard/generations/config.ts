// This is the route configuration for the server.ts to pick up when a request comes in
export default {
    // The route pattern for this endpoint (Express-style path params)
    route: '/v1/storyboard/generations/:storyId',
    delay: 0
};
